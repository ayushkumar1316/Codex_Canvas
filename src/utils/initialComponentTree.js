export const initialComponentTree = {
  id: "root",
  type: "root",
  props: {},
  styles: {
    minHeight: "100vh",
    display: "flex",
    flexDirection: "column",
    gap: "0px",
    backgroundColor: "#ffffff",
    color: "#0f172a",
    padding: "0",
    fontFamily:
      '-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Helvetica Neue", Arial, sans-serif',
  },
  children: [],
};

export default initialComponentTree;
